<%= form_with(model: flight, local: true) do |form| %>
  <% if flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

      <ul>
      <% flight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :flightTime %>
    <%= form.datetime_select :flightTime, id: :flight_flightTime %>
  </div>

  <div class="field">
    <%= form.label :source %>
    <%= form.text_field :source, id: :flight_source %>
  </div>

  <div class="field">
    <%= form.label :destination %>
    <%= form.text_field :destination, id: :flight_destination %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
